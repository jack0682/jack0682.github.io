# ──────────────────────────────────────────────────────────────
# jack0682.github.io — .gitignore
# Stack: Next.js 15 (App Router) + MDX, deployed to GitHub Pages
# ──────────────────────────────────────────────────────────────

# Private reference archive — NEVER commit (user explicit rule)
JaehongOh's_refenernce/

# ── Node / package managers ──
node_modules/
.pnp
.pnp.*
.yarn/install-state.gz
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── Next.js ──
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# ── Content build caches ──
.contentlayer/
.velite/
.turbo/

# ── Env / secrets ──
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# ── Deploy ──
.vercel/

# ── Testing / coverage ──
coverage/
*.lcov

# ── Jekyll legacy (in case _site/ resurfaces) ──
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
Gemfile.lock

# ── Editor / OS ──
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json

# ── Python venvs (may appear inside reference mirrors) ──
venv/
.venv/
__pycache__/
*.py[cod]

# ── Zone.Identifier (Windows-origin artifacts) ──
*:Zone.Identifier
PLAN_reference_integration.md